# Tests

The Python test suite covers strict metadata, adapters, execution, evaluation,
campaign integrity, metrics, reproduction, and publication. The root `justfile`
groups fast, unit, Docker, and full integration checks.
