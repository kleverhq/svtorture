# Suites

Suites are named, reviewable selections of case IDs used by campaign commands.
Their `cases` arrays contain glob patterns, allowing `["*"]` for the full corpus
and chapter-oriented patterns such as `["ch12-*"]`.
