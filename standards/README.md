# Standards catalog

`index.toml` lists maintained IEEE 1800 chapters, `requirements/` stores one
requirement document per chapter, and `tags.toml` defines the shared vocabulary
used by requirements and cases.
