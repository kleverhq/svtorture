# Deterministic fake tool

This image implements a stable test protocol used to exercise the real executor
and evaluator without depending on external compiler behavior. `fake_tool.py`
is copied into the image by the adjacent Dockerfile.
