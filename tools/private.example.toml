schema_version = 1

# Copy this file outside the repository (or to the gitignored
# tools/private.toml) and set SVTORTURE_TOOL_CONFIG to its path.
[[wrappers]]
tool = "vcs"
command = ["/absolute/path/to/private-vcs-wrapper"]
environment_allowlist = ["SNPSLMD_LICENSE_FILE"]
