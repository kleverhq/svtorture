# GitHub automation

This directory connects the repository's `just`-based checks and campaign
pipeline to GitHub Actions. See `workflows/` for PR, nightly, and Pages jobs;
workflow-specific guidance is in `workflows/AGENTS.md`.
