# Cases

Each child directory is one minimal, tool-neutral SystemVerilog experiment with
strict `case.toml` metadata. See [`docs/adding-a-case.md`](../docs/adding-a-case.md)
for the authoring workflow and `AGENTS.md` for local guidance.
