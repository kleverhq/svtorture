# Dashboard

This React application turns campaign datasets into a requirements matrix,
case-level evidence browser, history view, and campaign detail view. The default
example data comes from `../fixtures/dashboard/`; build commands live in the
root `justfile`.
