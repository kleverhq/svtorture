# Dashboard

This React application turns campaign datasets into a requirements matrix,
case-level evidence browser, history view, and campaign detail view. Vite builds
the static application without embedded evidence; `just dashboard-build`
exports selected campaigns into the ignored build tree.
